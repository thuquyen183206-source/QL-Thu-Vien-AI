{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "library:start",
            "type": "shell",
            "command": "docker compose up --build -d",
            "options": {
                "cwd": "${workspaceFolder}/library-ai-complete"
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}